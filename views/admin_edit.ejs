<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Editar reserva</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container mt-4">
    <a href="/admin/reservas" class="btn btn-outline-secondary mb-3">← Volver</a>
    <h2>Editar reserva #<%= reserva.id %></h2>

    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/admin/reservas/update/<%= reserva.id %>" method="POST">
      <div class="mb-3">
        <label class="form-label">Nombre *</label>
        <input class="form-control" type="text" name="cliente" required value="<%= reserva.cliente %>">
      </div>

      <div class="mb-3">
        <label class="form-label">Correo</label>
        <input class="form-control" type="email" name="email" value="<%= reserva.email %>">
      </div>

      <div class="mb-3">
        <label class="form-label">Teléfono</label>
        <input class="form-control" type="text" name="telefono" value="<%= reserva.telefono %>">
      </div>

      <div class="row">
        <div class="col-md-6 mb-3">
          <label class="form-label">Fecha *</label>
          <input class="form-control" type="date" name="fecha" required value="<%= reserva.fecha.toISOString().slice(0,10) %>">
        </div>
        <div class="col-md-6 mb-3">
          <label class="form-label">Hora *</label>
          <input class="form-control" type="time" name="hora" required value="<%= reserva.hora.slice(0,5) %>">
        </div>
      </div>

      <div class="mb-3">
        <label class="form-label">Personas *</label>
        <input class="form-control" type="number" name="personas" min="1" max="20" required value="<%= reserva.personas %>">
      </div>

      <div class="mb-3">
        <label class="form-label">Notas</label>
        <textarea class="form-control" name="notas" rows="3"><%= reserva.notas %></textarea>
      </div>

      <button class="btn btn-success" type="submit">Actualizar</button>
    </form>
  </div>
</body>
</html>