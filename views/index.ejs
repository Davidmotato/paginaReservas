<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Reservar - Restaurante</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
    <div class="container">
      <a class="navbar-brand" href="/">Restaurante & Co.</a>
      <div>
        <a class="btn btn-outline-secondary" href="/admin/reservas">Panel Admin</a>
      </div>
    </div>
  </nav>

  <main class="container">
    <div class="row align-items-center">
      <div class="col-md-6">
        <h1>Reserva tu mesa</h1>
        <p>Disfruta de la mejor experiencia gastronómica. Reserva con anticipación.</p>

        <% if (error) { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <form id="reservaForm" action="/reservar" method="POST" novalidate>
          <div class="mb-3">
            <label class="form-label">Nombre completo *</label>
            <input class="form-control" type="text" name="cliente" required value="<%= form.cliente || '' %>">
          </div>

          <div class="mb-3">
            <label class="form-label">Correo electrónico</label>
            <input class="form-control" type="email" name="email" value="<%= form.email || '' %>">
          </div>

          <div class="mb-3">
            <label class="form-label">Teléfono</label>
            <input class="form-control" type="text" name="telefono" value="<%= form.telefono || '' %>">
          </div>

          <div class="row">
            <div class="col-md-6 mb-3">
              <label class="form-label">Fecha *</label>
              <input class="form-control" id="fecha" type="date" name="fecha" required value="<%= form.fecha || '' %>">
            </div>
            <div class="col-md-6 mb-3">
              <label class="form-label">Hora *</label>
              <input class="form-control" id="hora" type="time" name="hora" required value="<%= form.hora || '' %>">
            </div>
          </div>

          <div class="mb-3">
            <label class="form-label">Personas *</label>
            <input class="form-control" type="number" name="personas" min="1" max="20" required value="<%= form.personas || 2 %>">
          </div>

          <div class="mb-3">
            <label class="form-label">Notas (alergias, peticiones)</label>
            <textarea class="form-control" name="notas" rows="3"><%= form.notas || '' %></textarea>
          </div>

          <button class="btn btn-primary" type="submit">Reservar ahora</button>
        </form>
      </div>

      <div class="col-md-6 d-none d-md-block">
        <img src="https://images.unsplash.com/photo-1541542684-9f770b1d8efb?q=80&w=1200&auto=format&fit=crop&ixlib=rb-4.0.3&s=..." alt="Comida" class="img-fluid rounded shadow">
      </div>
    </div>
  </main>

  <script src="/js/main.js"></script>
</body>
</html>